

const int relay_motor = 10; // the relay is connected to pin 2 of the Arduino board

#define LED_PIN 38
#define IR_SENSOR_PIN 7 // Assuming IR sensor is connected to pin 13


void setup() {
  pinMode(LED_PIN, OUTPUT);
  pinMode(relay_motor, OUTPUT);
  pinMode(IR_SENSOR_PIN, INPUT); // Set IR sensor pin as input
  Serial.begin(9600);
}



void loop() {
            digitalWrite(relay_motor, HIGH); // Turn the motor on

}
